<Page x:Class="ECN.Views.EcnSigned"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      DataContext="{Binding EcnSignedViewModel, Source={StaticResource Locator}}"
      mc:Ignorable="d" 
      d:DesignHeight="140" d:DesignWidth="350"
      Title="EcnSigned">

    <Grid Background="White">
        <Image Source="/Assets/checkgreen.png" Width="24" Height="24" VerticalAlignment="Top" Margin="0, 30, 0, 0" />
        <TextBlock Text="{Binding Message}" Margin="10, 60, 0, 10" VerticalAlignment="Top" HorizontalAlignment="Center" FontFamily="Quicksand Medium" FontSize="14" TextWrapping="Wrap"/>
    </Grid>
</Page>
